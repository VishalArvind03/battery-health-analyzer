[pytest]
# Where pytest should look for test files.
testpaths = tests
python_files = test_*.py
python_functions = test_*

# Custom markers keep the output clean and document special tests.
markers =
    nasa: tests that need the real NASA .mat files (auto-skipped if the files are not present)
